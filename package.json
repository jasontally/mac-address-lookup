{
  "name": "mac-address-lookup",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Free client-side MAC address lookup — identify the vendor behind any full or partial MAC address or OUI instantly in your browser.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node build/build.mjs",
    "build:data": "node build/build.mjs --data-only",
    "test": "node --test test/",
    "serve": "node build/serve.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hyparquet": "^1.30.1",
    "hyparquet-writer": "^0.16.9"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "wrangler": "^4.34.0"
  }
}
